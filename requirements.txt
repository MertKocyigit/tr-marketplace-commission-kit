# --- Runtime (required) ---
Flask>=3.0
Flask-Cors>=4.0
pandas>=2.2
numpy>=2.0
openpyxl>=3.1  # pandas.read_excel(.xlsx) engine
pdfplumber

# --- Optional (commented) ---
# camelot-py         # requires Ghostscript (system dep) for some PDFs
# tabula-py          # requires Java (system dep)
